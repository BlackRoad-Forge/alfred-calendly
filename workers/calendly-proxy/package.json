{
  "name": "calendly-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for Calendly API",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "tail": "npx wrangler tail"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "3.99.0"
  }
}
